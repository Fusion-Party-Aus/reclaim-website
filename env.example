# Environment variables for AEC CRM integration
# Copy to `.env` in development and set values.

# Base URL for the AEC CRM API (no trailing path). Example: https://crm.example.org
AEC_CRM_API_URL=

# Optional API key / bearer token for the CRM if required
AEC_CRM_API_KEY=

# Notes:
# - The site posts member payloads to `/members` on the CRM.
# - The local dev server must have these variables set for the proxy endpoint
#   at `src/pages/api/members.ts` to function.